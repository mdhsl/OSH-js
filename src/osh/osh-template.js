var OSH = {
	version: 'dev'
};

window.OSH = OSH;

OSH.Video = {
	version: 'dev'
};

window.OSH.Video = OSH.Video;

OSH.UI = {
	version: 'dev'
};

window.OSH.UI = OSH.UI;

OSH.DataSource = {
	version: 'dev'
};

window.OSH.DataSource = OSH.DataSource;

OSH.Utils = {
	version: 'dev'
};

window.OSH.Utils = OSH.Utils;
